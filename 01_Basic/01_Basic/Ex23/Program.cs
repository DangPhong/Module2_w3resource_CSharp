using System;

namespace Ex23
{
    class Program
    {
        //23. Write a C# program to convert a given string into lowercase.
        static void Main(string[] args)
        {
            string str = "nguyen DANG phong ";
            Console.WriteLine(str.ToLower());
            Console.WriteLine(str.ToUpper());
        }
    }
}
